// main.c - My entry point to test my math skills
// Gevorg: Bringing it all together with a personal flair

#include <stdio.h>
#include "math_utils.h"

int main() {
    int myNumber = 8; // I chose 8
    int myResult = computeSquare(myNumber);
    printf("Hey, the square of %d (my lucky number) is %d!\n", myNumber, myResult);
    
    return 0;

}
